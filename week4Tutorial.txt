Express in one file is a traffic 

so use router will help in maintainice and will not make traffic

module.exports creating a seperate module and exporting the router which contains the points

if any request arrive then send it to the router to handle it